node_modules/
dist/
site/
*-dist.zip
*.log
shots/

# Heavy local source artifacts (gazette PDFs + extracted text used by the
# gen:data pipelines); the parsed JSON is committed, so these stay local.
/*.pdf
/*.txt
